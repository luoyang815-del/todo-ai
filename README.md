# todo-ai (Android)

This repository snapshot provides a full-featured Android app baseline:
- Proxy/HTTPS/SOCKS5 with optional Basic auth
- Gateway base URL + TLS via Android Network Security Config (system+user CAs)
- Compose UI with Settings
- Notification on AI response
- DataStore persistence
- Debug signing by default
- CI workflow included
