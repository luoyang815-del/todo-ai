# 代理留空，后续在应用内填写
proxy:
  enabled: false
  type: http
  host: ""
  port: 0
  auth:
    enabled: false
    username: ""
    password: ""
gateway:
  enabled: true
  base_url: "https://gw.example.com"   # 占位
  tls:
    verify: true
sync:
  mode: manual
  interval_minutes: 30
notifications:
  enabled: true